package spec

// Leads paths (relative to /api/v1/leads mount)
const (
	ListURL   = "/"
	CreateURL = "/"
	ImportURL = "/import"
	GetURL    = "/{id:[0-9]+}"
	UpdateURL = "/{id:[0-9]+}"
	DeleteURL = "/{id:[0-9]+}"
)
